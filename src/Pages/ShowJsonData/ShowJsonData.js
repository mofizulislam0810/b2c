import React  from 'react';


const ShowJsonData = () => {
   
    return (
        <div>
            {
                // datas.map((data)=> data.directions.map(item=> item.map(item1=> item1.segments.map(item2 => (item2.details.map(item3 => console.log(item3)))))))
                // datas.map((data,index)=> console.log(index))
                // datas.map((data)=> data.directions.map(item1=> item1.map(item2=> item2.segments.map((item3,index) =>console.log(index)))))
                
            }
        </div>
    );
};

export default ShowJsonData;